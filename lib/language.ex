defmodule Lang do
  @moduledoc """
  # Elixir is a functional language

  Elixir is a powerful language because programs are just a mix of functions.
  This means that everything you'll see in this code are functions.
  Let's talk about functions.

  `add(a, b)`

  This function has the name "add". It receives two parameters. The first
  parameter is `a` and the second is `b`.

  # Learn common language

  functions.
  arguments,
  modules.

  """
end
